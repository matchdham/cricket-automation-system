{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "gunicorn --bind 0.0.0.0:8000 --workers 1 --timeout 60 backend.app:app"
  }
}
